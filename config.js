// Configuração global opcional para o front-end.
// Ajuste `apiBase` para apontar para o backend quando o site estiver hospedado
// em um domínio diferente (ex.: Netlify). Deixe vazio para usar a mesma origem.
window.SMART_API_CONFIG = window.SMART_API_CONFIG || {
    apiBase: '' // exemplo: 'https://seu-backend.com/api'
};
